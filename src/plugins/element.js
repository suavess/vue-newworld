import Vue from 'vue'
import { Button, Tabs, TabPane, Form, RadioGroup, RadioButton, FormItem, Input, Icon,
  Select, Option, Card } from 'element-ui'

Vue.use(Button).use(Tabs).use(TabPane).use(Form).use(RadioGroup).use(RadioButton).use(FormItem)
  .use(Input).use(Icon).use(Select).use(Option).use(Card)
